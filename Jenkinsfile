pipeline {
    agent any

     stages {
        stage('Compile') {
            steps {
                sh 'javac helooo.java'
            }
        }

        stage('Run Java') {
            steps {
                sh 'java helooo'
            }
        }

    }
}
